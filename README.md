# pedal

pedal is HTTP wrapper for PDAL which uses S3 for handling files

## Run inside conda

`conda create -n pdal -c conda-forge python-pdal python=3.9`

https://michhar.github.io/2023-07-poetry-with-conda/
`
poetry config virtualenvs.path {PATH to conda virtualenv}
poetry config virtualenvs.create false
`